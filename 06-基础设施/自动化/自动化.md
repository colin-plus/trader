---
type: 索引
created: 2026-08-11
updated: 2026-08-21
---

# 自动化

> Templater 脚本、插件配置、定时任务（cron）等自动化相关。

## 定时任务（cron）

> 由 **hermes gateway（trader profile）** 的 cron 子系统调度：任务定义在 `~/.hermes/profiles/trader/cron/jobs.json`，`no_agent` 模式直接执行 Python 脚本（不经模型），A 股收盘后按工作日触发。任务名沿用了初建时的"晶方+成都"（晶方 08-18 已清仓，脚本内已移除，**任务名尚未同步，改名可选**）。

### 任务 1：资金流向日更 — 15:40

- **调度**：`40 15 * * 1-5`（工作日 15:40，`jobs.json` id: `67d5e8dc27f7`）
- **脚本**：`~/.hermes/profiles/trader/scripts/update_fund_flow.py`（247 行）
- **数据源**：东方财富资金流接口 `push2his.eastmoney.com`（主力=超大单+大单，**第三方估算，仅供参考**）
- **更新对象**：`03-市场研究/个股档案/成都银行（601838）/资金流向.md`（晶方已清仓移除）
- **功能**：
  1. 重建资金流向表（主力/占比/超大/大/中/小单/收盘/涨跌/信号列，按日期去重全量重写）
  2. 更新「近 5 日趋势小结」（5 日净流合计 + 连续流入天数）
  3. 重写 Charts 图块（主力 vs 散户 0 轴分色 + 资金谱系 4 档，近 20 日，交互式）
- **幂等**：按日期去重，可重复运行
- **失败策略**：重试 3 次（间隔 15s×(i+1)）后 exit 1 → cron alert
- **⚠️ 维护注意**：持仓变化时更新脚本内 `STOCKS` 列表（secid/名称/文件路径）；东财限流严格，曾报 `Remote end closed connection`（08-17/08-18 成都银行失败），通常稍后重跑即恢复；中单/小单历史缺失时显示"待补"

### 任务 2：日度记录更新 — 15:45

- **调度**：`45 15 * * 1-5`（工作日 15:45，`jobs.json` id: `32d2d09911c0`）
- **脚本**：`~/.hermes/profiles/trader/scripts/update_daily_records.py`（97 行）
- **数据源**：腾讯 K 线接口 `web.ifzq.gtimg.cn`（稳定）
- **更新对象**：`04-复盘与计划/交易计划/交易计划-成都银行-2026-08-13.md` 的日度记录表（晶方已清仓移除）
- **功能**：在日度记录表末行后追加当日数据（开/高/低/收/成交量万手/振幅 + 当日盈亏/累积盈亏**近似值**，标注"（近似）"，以券商 APP 为准——库里不算账）
- **幂等**：已有日期跳过
- **⚠️ 维护注意**：持仓变化（买卖）后**必须**更新脚本内 `POSITIONS` 配置（code/pos/cost），否则盈亏近似值失真

### 执行记录与排障

- 任务定义：`~/.hermes/profiles/trader/cron/jobs.json`（cron 表达式/脚本名/enabled/next_run_at/last_status）
- 每次执行输出：`~/.hermes/profiles/trader/cron/output/<job_id>/<时间戳>.md`
- 最近状态（08-18）：日度记录 ok；资金流向 error（成都银行接口失败）——见上"东财限流"

## 手动工具脚本

### 笔记导出 PDF（md2pdf）

- **脚本**：`~/.hermes/profiles/trader/scripts/md2pdf.py`（md → HTML → weasyprint）
- **功能**：Obsidian 笔记导出 PDF——剥离 frontmatter、`[[wikilink]]` 转主题色标签（纯视觉不可点击）、中文排版 A4 页码
- **默认样式**：藏蓝主题 + HarmonyOS Sans SC 正文 + Fira Code 代码（连体已启用，`->`/`==` 等正常渲染）
- **用法**（miniconda python，需 `DYLD_LIBRARY_PATH=/opt/homebrew/lib`）：
  - 单篇：`python3 md2pdf.py <笔记.md> [输出目录] [--theme blue|green] [--font sans|print|fira|wenkai]`
  - 批量：`python3 md2pdf.py --dir <目录> [输出目录]`（自动跳过同名索引/下划线开头文件）
  - 默认输出：`~/Obsidian/PDF分享/`（批量建议指定子目录，如 `PDF分享/精读提炼/`）
- **⚠️ 局限**：Bases/Dataview/Charts 等 Obsidian 专有渲染块无法导出（仅纯 md 元素：标题/表格/引用/代码/列表）；带 `<mark>` 等 HTML 需自行转义
- **历史**：2026-08-21 为精读提炼定制（8 本全量导出）；样式参数经多轮调整定稿（H2 下边框、wl 标签、setext 空行陷阱已处理）

## 已配置（2026-08-14）

- **Periodic Notes**：周复盘/月复盘落点 `04-复盘与计划/复盘笔记`；weekly 格式 `复盘-YYYY-MM-DD`（取周一）、monthly 格式 `复盘-YYYY-MM`；模板 `06-基础设施/模板/复盘笔记模板`（period 自动算本周一）
- **Linter**：`lintOnSave` + `lintOnFileChange` 已开启；启用规则 `yaml-timestamp`（date-modified → `updated`，保存时自动刷新，无需手动跑）

## 已规划

- [ ] Templater 配置备忘
- [ ] 常用插件清单与配置
- [ ] cron 任务名同步（"晶方+成都" → 仅成都，晶方已清仓）
